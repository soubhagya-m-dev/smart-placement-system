# Copy to .env before `npm run build`. These are baked into the bundle at build time.

# Backend API base URL (NO trailing slash). Vite reads VITE_* at build time.
VITE_API_URL=https://your-backend.example.com
